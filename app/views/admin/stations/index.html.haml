%h1 Registrierte Lesestationen

%table.table.table-striped
  %colgroup
    %col
    %col{ width:80 }
    %col{ width:110 }
  %thead
    %tr
      %th Token
      %th
      %th

  - if @stations.any?
    %tbody
      - @stations.each do |station|
        %tr
          %td.monospace= link_to station.token, [:admin, station]
          %td= link_to 'Bearbeiten', edit_admin_station_path(station)
          %td= destroy_link_to [:admin, station]
  - else
    %tbody
      %tr.info
        %td{ colspan:3 }
          %p.text-center Keine Stationen angelegt.

= paginate @stations

= link_to 'Neue Lesestation', new_admin_station_path, class:'btn btn-primary'
