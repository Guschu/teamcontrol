= simple_form_for([:admin, @user]) do |f|
  = f.error_notification
  = f.input :email
  = f.input :password
  = f.input :password_confirmation

  = f.button :submit
  = link_to t('cancel'), admin_users_path, class:'btn btn-text'
